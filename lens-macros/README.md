# macro-lens-macros

`macro-lens-macros` implements the `lens!(...)` procedural macro used by `macro-lens`.

Most users should depend on `macro-lens` and import the macro from the re-exported `lens` crate:

```rust
use lens::lens;
```

## License

MIT.
